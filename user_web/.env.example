# Explicit API URL override (optional).
# If empty, frontend computes:
# - dev (`npm run dev`):  http://<browser-host>:VITE_API_DEV_PORT
# - prod (docker reverse proxy): same-origin (https://<your-domain>)
VITE_API_ADDRESS=""
VITE_API_DEV_PORT="8070"
