# install this as a package from root directory, so it can be used by all python microservices.

pip install -e ./__library

<!-- and use as from library import models -->
<!-- That is an editable install → changes in __library/library are picked up automatically. -->